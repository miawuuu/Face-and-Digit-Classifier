Implemented a three-layer machine learning neural network with Python and PyTorch that
classifies given data as a face or digit or neither. Compared accuracy and results with different amounts of training data and
with the perceptron algorithm.
